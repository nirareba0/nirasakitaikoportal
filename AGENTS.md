# 実装規約（Agy 用）

Agy はこのファイルと `TASKS.md`、渡された指示書を読んで実装する。Claude Code はこのファイルを自動で読まないので、実装の細部はここに書いてよい。

## 技術スタック
- 素のHTML5 / CSS3 / vanilla JS のみ。フレームワーク・ビルドツール・npm依存なし
- ページ: index.html / about.html / videos.html / news.html / links.html（共通CSS: assets/style.css、共通JS: assets/script.js）
- 動画は YouTube iframe embed（`https://www.youtube.com/embed/<videoId>`）を直接使う

## テスト・ビルド・確認コマンド
- テスト: 自動テストなし。`python3 -m http.server 8080` でローカル起動し、トップ〜全ページのリンクが辿れるか目視確認
- lint/型: なし（HTMLは手で構文を確認。壊れた閉じタグが無いこと）
- 起動/確認: 上記の `http.server` でブラウザ確認。スマホ幅(375〜400px)と PC幅の両方をブラウザのレスポンシブモードで確認する

## ディレクトリ構成と触ってよい範囲
- 触ってよい: リポジトリ直下の `*.html`、`assets/`
- 触ってはいけない: `REQUIREMENTS.md` / `DESIGN.md`（上流ドキュメント。矛盾を見つけたら書き換えず報告して止まる）

## やってはいけないこと
- git commit / push をしない
- 指示書に無いファイルを編集しない。必要なら報告して止まる
- 数値や仕様を「良さそう」で変えない。根拠（実測・仕様書）を示す
- 存在しない関数・モジュールを import したまま終わらない。終了前に必ずテストか実行で確認する
- REQUIREMENTS.md の「確認済み事実」に無い活動内容・数値・エピソード・写真を創作/転載しない
- 学校公式サイト（nirasakith.kai.ed.jp）の画像ファイルをダウンロードして本サイトに掲載しない。写真が欲しい箇所はCSS装飾かYouTube埋め込みのサムネイルで代替する
- Instagram埋め込み（oEmbed等）は使わない。「Instagramで見る」という外部リンクのみにする

## サブエージェントの使い方
- 探索・調査は `research`（読み取り専用）に出してよい
- 実装後のテスト実行は `verifier` に出し、結果を要約ではなく原文で受け取る
- 差分の自己点検は `reviewer` に出してよい
- 同じファイルを編集する子を並列に作らない。編集は親（あなた）だけが行う

## 完了時の報告
- 変更したファイルの一覧、実行したテストコマンドとその結果（原文）、自分で確認できなかった点
- `TASKS.md` の該当項目にチェックを付け、詳細は `CHANGELOG.md` に1〜2行
